name: Frappe Docker Development Build and Deploy for BSP Engineering

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to deploy"
        required: true
        default: "develop"
        type: string

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          repository: Excel-Technologies-Ltd/excel-frappe-docker

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitLab Container Registry
        uses: docker/login-action@v3
        with:
          registry: registry.gitlab.com
          username: ${{ secrets.GITLAB_USERNAME }}
          password: ${{ secrets.GITLAB_TOKEN }}

      - name: Build and Push Frappe Docker Image
        run: |
          # Define apps JSON (write to file for BuildKit secret — avoids token leak via build args)
          cat > /tmp/apps.json <<'EOF'
          [
            {
              "url": "https://github.com/frappe/erpnext",
              "branch": "version-15"
            },
            {
              "url": "https://github.com/frappe/hrms.git",
              "branch": "version-15"
            },
            {
              "url": "https://gitlab.com/pixfar/whitelabel.git",
              "branch": "master"
            },
            {
              "url": "https://github.com/pixfar/flex_erp.git",
              "branch": "master"
            },
            {
              "url": "https://github.com/frappe/lending.git",
              "branch": "version-15"
            },
            {
              "url": "https://github.com/pixfar/bsp_engineering.git",
              "branch": "develop"
            },
            {
              "url": "https://github.com/pixfar/POS-Awesome-V15",
              "branch": "master"
            }
          ]
          EOF

          # Define image tag
          IMAGE_TAG="registry.gitlab.com/pixfar/pixfar-container-registry/erp-all-demo:bsp"

          # Build and push to registry using BuildKit secret for apps.json
          docker buildx build \
            --no-cache \
            --platform linux/amd64 \
            --build-arg FRAPPE_PATH=https://github.com/frappe/frappe \
            --build-arg FRAPPE_BRANCH=version-15 \
            --build-arg PYTHON_VERSION=3.11.9 \
            --build-arg NODE_VERSION=20.19.2 \
            --secret id=apps_json,src=/tmp/apps.json \
            --tag "$IMAGE_TAG" \
            --file images/custom/Containerfile \
            --push .
